---
scope: BeansJourney MVP: Laravel 13 + Inertia/React client, Filament admin, for coffee bean reviews & recipes
purpose: build-substrate
altitude: feature
updated: 2026-07-31T11:31
---

- (direction) User chose Fast Path (draft with [ASSUMPTION] tags, review after) and spine-only deliverable (no extra discussion doc/diagram deck). Input: SPEC-beansjourney (8 capabilities, 4 constraints, 3 non-goals) plus its open question on relational vs NoSQL for threaded content
- (decision) Paradigm: Layered MVC with an Application Service layer, Laravel-idiomatic (Eloquent models double as domain layer per Laravel convention [ADOPTED]). Two presentation surfaces (Inertia/React client, Filament admin) share one Application/Domain layer -- not two separate backends
- (version) PHP 8.3+ required by Laravel 13 (verified web: Laravel 13 released March 2026, min PHP 8.3) -- Binds: runtime for all capabilities
- (version) Laravel 13 (per user constraint, verified current release March 2026 via web)
- (version) Filament v5 (verified web: ships aligned with Laravel 13; Filament 3 also Laravel 10+ compatible but v5 is current for new Laravel 13 projects) -- Binds: CAP-8 admin console
- (version) Inertia.js protocol v3 -- server adapter inertiajs/inertia-laravel, client @inertiajs/react ^3.6 (verified web: Inertia v3 released March 2026, npm current 3.6.1) -- Binds: client-facing capabilities per user constraint
- (version) React 19.2 (verified web: current stable as of July 2026) -- Binds: client-facing capabilities
- (decision) AD-1 data engine: Binds all persistence-backed capabilities (CAP-1..CAP-8). Prevents: relational vs NoSQL split-brain across services for the same threaded content. Rule: single relational store, MySQL 8.0+, is the system of record; threaded comments modeled relationally (adjacency list), not a document store. [ASSUMPTION] engine=MySQL vs PostgreSQL not specified by user -- MySQL chosen as Laravel's most common default for simple hosting; PostgreSQL is an equally valid substitute, revisit if full-text search is added later. This resolves the user's open question from SPEC-beansjourney: relational is sufficient at MVP scale
- (decision) AD-2 comment model: Binds CAP-3 (review threads), CAP-4 (recipe threads). Prevents: two separate comment implementations (review-comments vs recipe-comments) drifting in shape, moderation, or upvote logic. Rule: single polymorphic comments table (commentable_type/commentable_id -> Review or Recipe) with nullable parent_id self-reference for nested replies; both thread types reuse the same table, policy, and service
- (decision) AD-3 vote model: Binds CAP-3, CAP-4. Prevents: duplicate or divergent per-entity vote tables letting a user vote twice on the same item via different code paths, or inconsistent uniqueness rules across votable types. Rule: single polymorphic votes table (votable_type/votable_id -> Review, Recipe, or Comment) + user_id, unique constraint on (votable_type, votable_id, user_id); voting again toggles/removes the vote
- (decision) AD-4 shared application layer across two presentation surfaces: Binds all capabilities, especially CAP-8. Prevents: admin-side mutations (Filament actions) reimplementing or diverging from client-side business rules for the same entity (Bean, Review, Recipe, Comment, User). Rule: all writes to shared entities go through Application Services (plain-argument, no Http\Request dependency) that both Inertia controllers and Filament resources call -- neither surface mutates Eloquent models directly for these entities
- (decision) AD-5 auth boundary: Binds CAP-5 (register/verify), CAP-6 (password reset), CAP-8 (admin access). Prevents: a verified regular user reaching the admin panel, or ambiguity about which guard governs which surface. Rule: Laravel's native auth (Fortify/Breeze conventions) with MustVerifyEmail on the web guard shared by Inertia; users.role enum ('user'|'admin'); Filament panel canAccessPanel() requires role=admin; all mutating client routes (create bean/review/recipe/comment/vote) gated by auth + verified middleware per the spec's constraint that only registered, logged-in users may create content
- (decision) AD-6 dependency direction: Binds all. Prevents: Domain (Models/Policies) depending on Http or Filament namespaces, or Services becoming unusable from Filament because they expect an Http\Request. Rule: Http Controllers and Filament Resources both depend on Application Services; Services depend on Models/Policies; Models never depend upward. Diagram: HttpControllers-->Services, FilamentResources-->Services, Services-->Models, Models-->MySQL
- (decision) Consistency conventions -- naming: snake_case DB tables/columns, PascalCase singular Eloquent models (Bean, Roastery, Review, Recipe, Comment, Vote, User), kebab-case Inertia routes with matching resources/js/Pages folder names, Filament resources named {Model}Resource
- (decision) Consistency conventions -- data & formats: bigint auto-increment PKs, UTC timestamps via Eloquent created_at/updated_at, dates serialized ISO 8601 to the client, validation errors via Laravel's standard Inertia shared errors prop (no custom envelope)
- (decision) Consistency conventions -- state & cross-cutting: all writes to Bean/Roastery/Review/Recipe/Comment/Vote route through Services (never direct from controllers/Filament actions); one Policy per model checked in both Http controllers (->authorize) and Filament resource can* hooks; verified-email requirement enforced via the verified middleware alias on every mutating client route
- (decision) Structural seed -- deployment & environments [ASSUMPTION, not specified by user]: single Laravel deployable across local (Sail/Docker) and production environments via .env; mail via any SMTP-compatible provider through Laravel Mail for verification/reset emails; queue driver starts as 'database' (sync-safe, no extra infra) and can move to Redis later; specific hosting provider left open
- (decision) Core-entity ERD: Roastery 1--* Bean; Bean 1--* Review; Bean 1--* Recipe; User 1--* Review, 1--* Recipe, 1--* Comment, 1--* Vote; Comment polymorphic to (Review, Recipe) plus self-referencing parent_id; Vote polymorphic to (Review, Recipe, Comment)
- (decision) Minimal source tree: app/Http/Controllers (Inertia, thin), app/Http/Requests (Form Requests), app/Filament/Resources (admin CRUD), app/Models (Bean, Roastery, Review, Recipe, Comment, Vote, User), app/Policies, app/Services (CreateReview, CreateRecipe, ToggleVote, ...); resources/js/Pages + Components (Inertia/React); database/migrations, factories, seeders; routes/web.php (Inertia) + Filament panel provider registers its own routes
- (decision) Capability -> Architecture map: CAP-1,2 -> Models/Roastery,Bean + Http/Controllers/BeanController + Pages/Beans/* (AD-1, paradigm); CAP-3 -> Models/Review + Services/ReviewService (AD-2, AD-3); CAP-4 -> Models/Recipe + Services/RecipeService (AD-2, AD-3); CAP-5,6,7 -> Http/Controllers/Auth/*, ProfileController (AD-5); CAP-8 -> Filament/Resources/* (AD-4, AD-5)
- (direction) Deferred: MySQL vs PostgreSQL choice (see AD-1 assumption); search/filter over beans (not requested by any capability); rate limiting/spam moderation policy beyond manual admin moderation; deployment target/provider; image/photo upload for beans or roastery; in-app notifications beyond transactional email; role system beyond simple user/admin tier -- none requested by SPEC-beansjourney capabilities, each revisit-on-demand
- (event) Fast-path draft complete, proceeding to distill ARCHITECTURE-SPINE.md from this memlog
- (event) Reviewer gate ran: rubric+reconcile, version-verification, adversarial-incompatibility -- 3 parallel subagents, full reviews in reviews/review-{rubric,versions,adversarial}.md. Verdicts: pass-with-notes / discrepancies found / 8 incompatible-pair holes. Applying clear fixes below; genuinely open items pushed to Deferred/Open Questions
- (version) FIX (versions review): MySQL 8.0 reaches EOL April 2026 -- replaced with MySQL 8.4 LTS (supported to 2032) in AD-1 and Stack
- (version) FIX (versions review): React pinned to ^19.2.1+ (19.2.0 and earlier 19.x carry CVE-2025-55182, RSC/Flight-protocol RCE; this Inertia client-React stack doesn't use RSC so likely non-exploitable, but pin the floor and note the mitigation explicitly rather than leaving it silent)
- (version) FIX (versions review): Filament pinned to v5.7.4+ minimum (earlier v5 releases had an illuminate/contracts conflict with Laravel 13 that was only later resolved)
- (decision) FIX AD-3 (adversarial finding: vote schema ambiguity) -- tightened: votes are upvote-only per the spec (no downvote), so the votes table needs no value/direction column; a row's presence IS the vote, casting again deletes the row (toggle-off), never flips a value. Binds CAP-3, CAP-4
- (decision) FIX AD-2 (adversarial finding: comment deletion semantics undefined on the shared table) -- tightened: comments use soft delete (deleted_at) only, never hard/cascade delete; a deleted comment's replies remain visible with the parent rendered as a placeholder ('[deleted]') so nested threads never orphan. Binds CAP-3, CAP-4
- (decision) FIX AD-4 (adversarial finding: 'no direct model mutation' has no enforcement mechanism -- Filament v5's default scaffolded CRUD mutates Eloquent directly unless overridden, and DB-level ON DELETE CASCADE would bypass Services entirely even from compliant code) -- tightened with two new rule clauses: (1) every Filament Resource for a governed entity (Bean, Roastery, Review, Recipe, Comment, User role field) MUST override its create/update/delete lifecycle hooks to call the matching Application Service rather than relying on Filament's default direct Eloquent save/delete; (2) foreign keys from comments/votes to their parents use RESTRICT or application-level cleanup, never ON DELETE CASCADE, so deletion side-effects (counters, soft-delete cascades, notifications) always run through a Service
- (decision) FIX AD-5 + new AD-7 (adversarial finding: Bean/Roastery ownership contradiction -- AD-5's rule implied users can create beans per the spec constraint, but the ERD had no User->Bean relation and CAP-1/2 weren't bound to AD-5, so two stories could pick opposite owner models). New AD-7 -- bean/roastery authorship: Binds CAP-1, CAP-2, CAP-8. Prevents: divergent assumptions about who may create/edit a Bean or Roastery once published. Rule: any verified user MAY create a new Bean (and its Roastery via find-or-create-by-name if it doesn't exist yet), recorded via Bean.created_by/Roastery.created_by; after creation, editing an existing Bean/Roastery is admin-only via Filament (CAP-8) -- the creating user does not retain standing edit rights, matching the spec's silence on a user-facing 'edit my bean' capability. ERD gains User 1--* Bean (created_by) and User 1--* Roastery (created_by)
- (decision) FIX (adversarial finding: author-vs-admin delete authority on reviews/recipes/comments was undefined) -- clarified under AD-4/AD-5: the owning Service's delete method accepts an actor; a Review/Recipe/Comment author may soft-delete their own post, an admin may soft-delete any post via Filament -- same Service method, different actor context, both paths still route through the Service per AD-4
- (decision) FIX (adversarial finding: 'verified' middleware scope was stated only for 'create' in AD-5's prose) -- clarified: the auth+verified gate applies to every mutating client route -- create, update, delete, and vote-toggle -- for beans, reviews, recipes, comments, and votes, not just initial creation
- (decision) FIX (rubric finding, HIGH: CAP-8's 'user activity history log' had no architectural grounding) -- new decision: user activity history is recorded via Laravel's model-events (a lightweight 'activity_log' table capturing actor, action, subject type/id, timestamp) written by the Application Services layer (consistent with AD-4 -- Services already sit on every mutation path, so logging lives there, not bolted on separately); surfaced to admins as a Filament Resource. Binds CAP-8
- (decision) FIX (rubric finding, MEDIUM: CAP-4's structured recipe fields -- brew method, tools -- were spec-mandated but architecturally ungoverned) -- new decision: Recipe.brew_method is a fixed enum (americano, espresso, v60, french_press, aeropress, tubruk, other) per the spec's example list; Recipe.tools is a JSON column (machine/grinder/dripper as free key-value pairs) rather than a normalized Tool entity -- simplest shape that satisfies CAP-4 at MVP scale, revisit if tools need to become filterable/structured later. [ASSUMPTION] exact tools shape not specified by user beyond examples
- (decision) FIX (rubric finding, MEDIUM/LOW: no Comment-moderation Filament resource; analytics mapped to the wrong structural bucket) -- source tree gains app/Filament/Resources/CommentResource.php and app/Filament/Widgets/ (analytics widgets, not a Resource); Capability -> Architecture Map's CAP-8 row updated to point at both Filament/Resources/* and Filament/Widgets/*
- (event) All HIGH and MEDIUM reviewer findings from the three review files applied as tightened/new ADs above. Remaining LOW items and genuinely-open items (exact Filament version pin beyond v5.7.4, MySQL vs Postgres, deployment provider) stay in Deferred / open as assumptions -- none are unsafe to leave open for a fast-path MVP spine
- (event) Re-distilling ARCHITECTURE-SPINE.md with fixes applied (AD-1..AD-6 tightened in place, AD-7 added, ADs never renumbered)
- (event) spine finalized -- status: final, updated: 2026-07-31
